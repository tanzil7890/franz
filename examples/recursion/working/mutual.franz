// Mutual recursion without any special keyword

is_even = {n -> <- (if (is n 0) {<- 1} {<- (is_odd (subtract n 1))})}
is_odd  = {n -> <- (if (is n 0) {<- 0} {<- (is_even (subtract n 1))})}

(println "is_even 4 =" (is_even 4))
(println "is_odd 7 =" (is_odd 7))

