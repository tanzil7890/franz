// Test closures without refs

(println "=== Simple Closure Test ===")

(println "Test 1: Simple closure without ref")
make_adder = {n ->
  <- {x ->
    <- (add n x)
  }
}

add5 = (make_adder 5)
result = (add5 10)
(println "Result:" result)

(println "Done!")
